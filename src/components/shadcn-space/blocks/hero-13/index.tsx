import HeroSection from "@/components/shadcn-space/blocks/hero-13/hero";

/**
 * About hero only - site chrome (navbar-02) lives in root layout.
 * BrandSlider / logo marquee intentionally omitted; stats strip is in hero.tsx.
 */
const Hero13About = () => {
  return <HeroSection />;
};

export default Hero13About;
